import { View, Text } from 'react-native';

export default function IdentityScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-xl font-semibold">Identity</Text>
    </View>
  );
} 